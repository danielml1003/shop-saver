# Copy this file to .env and fill in your values before running docker compose.

# PostgreSQL password (choose something strong)
POSTGRES_PASSWORD=changeme

# External port the app is served on (80 = standard HTTP)
APP_PORT=80
